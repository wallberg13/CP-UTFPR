<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sir_berg.prova01_dispositivos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PesoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AlturaActivity" />
        <activity android:name=".SexoActivity" />
        <activity android:name=".IdadeActivity" />
        <activity android:name=".CodigoActivity" />
        <activity android:name=".ListarCodActivity" />
        <activity android:name=".ResultadoActivity"></activity>
    </application>

</manifest>