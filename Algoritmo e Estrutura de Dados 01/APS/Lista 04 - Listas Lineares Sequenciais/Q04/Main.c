/**
   Crie uma função que inverta a ordem dos elementos de uma lista. 
   A função inverte deve possuir apenas a lista alvo,
   ou seja, o parâmetro deve ser alterado.
 */
#include<Wall.h>
#include<stdio.h>

int main(){
  Lista lista;
  
} 

int inverteOrdem(Lista *lista){
}
